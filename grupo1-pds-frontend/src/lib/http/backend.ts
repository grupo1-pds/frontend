export const deleteHelper = async (helperId: string) => {
  console.log(helperId)
  // await fetch(`http://localhost:3000/helpers/${helperId}`, {
  //   method: 'DELETE'
  // })
}

export default deleteHelper